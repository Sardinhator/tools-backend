spring:
  application:
    name: tools-backend

  profiles:
    active: local

app:
  security:
    authentication:
      enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console
  vin-decoder:
    enabled: true
    base-url: https://vin-decoder-stg.intelcom.ca
    api-key: maULSvz4lNeHRmQBnXioubn5GZxf3POWaETRC3w7U5c7csKx
    connection-timeout: 5000
    read-timeout: 10000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
